<header>
    <div><a href="/">Casa do Hardware</a></div>
    <nav>
        <ul>
            <% if (locals.isAuth) { %>
                <li>
                    <form action="/logout" method="POST">
                        <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
                        <button>Sair da conta</button>
                    </form>
                </li>
            <% } %>
        </ul>
    </nav>
</header>